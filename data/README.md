# Data

`role_edges.json` is a compact cache derived from AniList character, media, and
Japanese voice-credit metadata. It intentionally excludes the large exploratory
research dumps used to create it.

Anime links use MAL when `mal_anime_id` is available. Character and seiyuu links
use AniList because this compact cache does not include MAL character/person ids.
